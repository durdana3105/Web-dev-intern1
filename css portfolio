body {
    font-family: Arial, sans-serif;
    background-image: url("brown.avif");
    margin: 0;
    padding: 0;
}

header {
    background-color: #4f0404;
    color: #fff;
    padding: 1em;
    text-align: center;
}

nav ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: space-between;
}

nav li {
    margin-right: 20px;
}

nav a {
    color: #e2c5db;
    text-decoration: none;
}

main {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 2em;
}

section {
    background-color: #a894a8;
    padding: 2em;
    margin-bottom: 20px;
    box-shadow: 5px 5px 25px;
    border-radius: 50px;
    

h1 {
    font-size: 24px;
    margin: 0;
    text-shadow: 4px color(srgb red green blue);
    
}

ul {
    list-style: none;
    margin: 0;
    padding: 0;
}

li {
    margin-bottom: 20px;
}

img {
    width: 100%;
    height: 150px;
    object-fit: cover;
    margin-bottom: 10px;
}

form {
    display: flex;
    flex-direction: column;
    align-items: center;
}

input, textarea {
    padding: 10px;
    margin-bottom: 20px;
    border: none;
    border-radius: 5px;
}

button {
    background-color: #333;
    color: #fff;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

button:hover {
    background-color: #444;
}
